<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cmps297.fridgecheck">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fridgecheck">
        <activity android:name=".itemsPageAdm"></activity>
        <activity android:name=".itemsPage" />
        <activity android:name=".editItems" />
        <activity android:name=".addItem" />
        <activity android:name=".wishList" />
        <activity android:name=".About" />
        <activity
            android:name=".PopCall"
            android:theme="@style/Theme.Customtheme" />
        <activity android:name=".Profile" />
        <activity android:name=".Settings" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>